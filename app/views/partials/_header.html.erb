<% url = request.fullpath.split('/') %>
<div class="table-cell<% if request.fullpath == '/' %> home<% end %>">
	<div class="wrapper<% if url[3] == 'edit' %> edit<% end %>">
		<% if url[3] != 'edit' && url[2] != 'new' %>
		<header role="banner">
			<h2>
				Contentboard
				<span class="icon-file-text-alt"></span>
			</h2>
			<% if request.fullpath == '/' %>
			<!--<h3>
				<a href="#">My boards</a>
			</h3>-->
			<small>Create, edit, and share web content easily.</small>
			<div class="grad"></div>
			<% end %>
		  <%= image_tag('/assets/typewriter.jpg') %>
		</header>
		<% end %>